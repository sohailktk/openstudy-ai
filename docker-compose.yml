services:
  openstudy:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./data/openstudy.db
      MAX_UPLOAD_MB: 25
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    restart: unless-stopped
